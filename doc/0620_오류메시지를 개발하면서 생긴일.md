1. 오류메시지창을 만들다보니 videoToSTT.jsx파일에서 만 작성하면, 다른 화면에서도 못쓴다는 사실이다. 
2. 오류메시지 창은 공통으로 관리가 되면, 다른 화면에서도 쓸수 있고 이는 YoutubeDebugingUI.jsx파일로 코드를 옮겼다. 
3. 오류메시지 창을 메시지만 전달하게 했다. 성공함. 
이를 위해 

`
export default function YoutubeDubbingUI() {
  ...
  const [errorTitle, setErrorTitle] = useState("");  
  const [errorMessage, setErrorMessage] = useState("");
  const handleError = (errorObject) => {
    setErrorTitle(errorObject.title);
    setErrorMessage(errorObject.message);
  };

  return (
        ...
        <main className="page-shell">
            ...
            
            {activePage === MENU_KEYS.EXPORT_STT && <VideoToSTT onError={handleError}/>}
            
            ...
            <ErrorPopup
              open={!!errorMessage}
              title={errorTitle}
              message={errorMessage}
              onClose={() => setErrorMessage("")}
            />
            ...
        </main>
        ...
  );
}
```


```
   export default function VideoToSTT({ onError }) {
   ...
      const handleError = (errorObject) =>{

        onError?.(errorObject);
    };

    
    <button className="start-button" 
                      onClick={() => handleError({title:"info",message:"업로드에 실패했습니다"})}>
        오류버튼 테스트
    </button>
   ...
   }

```
4. 3번 방식대로 안하면, 많은 오류에 시달릴 가능성이 크다. 
특히 object하나만 던져주면, 여러가지 정보를 표현할 수 있기 때문에 Object전달로 개선함.

