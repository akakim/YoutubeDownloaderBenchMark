
[문제점] 원래는 NodeJS 안에 파일 업로드, 파일을 mp3로 변환, mp3를 STT를 활용하여 SRT파일로 내려주는 것이 기획이였음.

개발이 진행함에 따라. 
1. NodeJS의 Spwan으로 Python의 STT를 실행히키는건 오류 코드를 제대로 받아볼 수 없는 문제가 생김.
이를 해결하기 위해 Python은 FastAPI로 웹 어플리켄끼리 통신으로 구조 변경

2. 1번으로 수정하니 기존의 Job은 backend/upload/jobs에서 관리하는 파일이였음.
3. FastAPI에서 접근이 용이하도록 {프로젝트경로}/jobs로 수정함.

4. 하지만 NodeJS에서 jobs경로가 바뀜에 따라 관리의 어려움이 생김.
이를 해결하기 위해 {프로젝트 경로}/jobs를 관리하는 VideoToTextRepository.js파일을 생성하여
